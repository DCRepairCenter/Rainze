# Ruff 配置说明 / Ruff Configuration Note
# 主配置已集成到 pyproject.toml，本文件为可选的独立配置
# Main config is integrated into pyproject.toml, this file is optional standalone config

# 如需独立配置，取消下方注释 / Uncomment below for standalone config
# [lint]
# select = ["E", "W", "F", "I", "B", "C4", "UP"]
